{
  "//": "Emits to dist so apps consume compiled JS (clean container entrypoints). @hub/shared resolves via node_modules to its built types — no path alias — so this package only compiles its own src (rootDir stays clean).",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "lib": ["ES2022"]
  },
  "include": ["src"]
}
